{
  "name": "ton-bridge-circuits",
  "version": "1.0.0",
  "description": "ZK Circuits for TON-Solana Bridge",
  "scripts": {
    "build:ton": "echo 'Mock circuit build - circom not available'",
    "setup": "echo 'Mock setup'",
    "compile:ton": "echo 'Mock compilation'",
    "test:ton": "node scripts/test_ton_verifier.js",
    "generate:input": "node scripts/generate_ton_inputs.js"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "ffjavascript": "^0.3.0",
    "snarkjs": "^0.7.0"
  },
  "devDependencies": {
    "mocha": "^10.0.0"
  }
}
