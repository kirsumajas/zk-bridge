version: '3.8'

services:
  solana-validator:
    image: solanalabs/solana:v1.17.17
    ports:
      - "8899:8899"
      - "8900:8900"
    command: |
      solana-test-validator \
        --rpc-port 8899 \
        --ws-port 8900 \
        --reset \
        --quiet
